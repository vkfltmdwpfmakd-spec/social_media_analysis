# 소셜 미디어 감성 분석 프로젝트 환경변수 예시
# 실제 사용 시 .env 파일로 복사하고 값을 수정하세요

# === 환경 설정 ===
ENVIRONMENT=development  # development, testing, production
LOG_LEVEL=DEBUG         # DEBUG, INFO, WARNING, ERROR, CRITICAL

# === Reddit API 설정 (필수) ===
REDDIT_CLIENT_ID=your_reddit_client_id_here
REDDIT_CLIENT_SECRET=your_reddit_client_secret_here
REDDIT_USERNAME=your_reddit_username_here
REDDIT_PASSWORD=your_reddit_password_here
REDDIT_USER_AGENT=social_media_analysis:v2.0 (by /u/your_username)

# === 감성 분석 API 키 (고도화 기능) ===
# OpenAI GPT API (최고 품질 감성 분석)
OPENAI_API_KEY=sk-your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# Hugging Face API (무료 대안)
HUGGINGFACE_API_KEY=hf_your_huggingface_token_here

# === 인프라 설정 ===
# Kafka
KAFKA_BROKER=kafka:9092
KAFKA_TOPIC=reddit-stream

# HDFS
HDFS_NAMENODE_HOST=namenode
HDFS_NAMENODE_PORT=8020
HDFS_WEB_PORT=9870

# Spark
SPARK_MASTER_HOST=spark-master
SPARK_MASTER_PORT=7077

# === 성능 튜닝 설정 ===
# Spark 메모리 설정
SPARK_EXECUTOR_MEMORY=4g
SPARK_DRIVER_MEMORY=2g
SPARK_EXECUTOR_CORES=4

# API 제한
MAX_API_REQUESTS_PER_MINUTE=50
API_TIMEOUT_SECONDS=10

# 배치 처리
SPARK_BATCH_INTERVAL=30
MAX_FILES_PER_LOAD=15
PARQUET_COMPRESSION=snappy

# === 모니터링 설정 ===
HEALTH_CHECK_INTERVAL=60
DASHBOARD_REFRESH_SECONDS=30

# 알림 설정
ALERT_EMAIL=admin@your-domain.com

# === 로깅 설정 ===
LOG_FILE_PATH=/var/log/social_media_analysis

# === 데이터 수집 설정 ===
SUBREDDIT_NAME=all

# === 프로덕션 전용 설정 ===
# (ENVIRONMENT=production일 때만 사용)

# SSL/TLS 설정
SSL_CERT_PATH=/path/to/ssl/cert.pem
SSL_KEY_PATH=/path/to/ssl/key.pem

# 보안 키
SECRET_KEY=your_very_secure_secret_key_for_production
JWT_SECRET=your_jwt_secret_key_for_authentication

# 외부 서비스 URL (프로덕션)
EXTERNAL_KAFKA_BROKER=kafka.production.com:9092
EXTERNAL_HDFS_HOST=hdfs.production.com

# 모니터링 도구
PROMETHEUS_ENDPOINT=http://prometheus:9090
GRAFANA_ENDPOINT=http://grafana:3000

# === 개발 도구 설정 ===
# (개발 환경에서만 사용)
ENABLE_DEBUG_LOGGING=true
DISABLE_API_RATE_LIMITING=true
USE_MOCK_APIS=false